/*

This is your global styles file. Selectors and rules you define
here will be applied throughout your app. However, these rules
have the lowest priority of any style settings.

For more information, see the "Style Priorities" section of
http://docs.appcelerator.com/platform/latest/#!/guide/Alloy_Styles_and_Themes

For example, the following would apply to all labels, windows,
and text fields (depending on platform) in your app unless you
overrode the settings with other TSS, XML, or JS settings:

'Label[platform=android,windows]': {
	color: '#000' // all platforms except Android and Windows default to black
}

'Window': {
	backgroundColor: '#fff' // white background instead of default transparent or black
}

'TextField[platform=android]': {
	height: Ti.UI.SIZE
}

*/

".view123[platform=ios formFactor=tablet]": {
  	borderColor: "#FFFFFF",
    	top: "40px",
    width: "293",
    	height: "40",

      	fontWeight: "5"

}

    ".view123[platform=android ]" : {
    	borderColor: "#FFFFFF",
    	left: "100px",
    	top: "40px",
    	right: "100px",

    	height: "40",
    	fontWeight: "5"
    }
    ".buttonContainer[platform=ios formFactor=tablet]": {
      width: "293",

    }

        ".buttonContainer[platform=android ]" : {
          left: "100px",
        right: "100px",
        }




".NeoSTORE" : {
    color: "#FFFFFF",
    font: {
        fontSize: 45,
        fontWeight: "bold",
        fontFamily: 'ufonts.com_gotham-bold-1-opentype',
    },
    text: "NeoSTORE"
}
".container[platform=android ]" : {
	backgroundImage: "imgpsh_fullsize.png",
  top: "0"
}
".container[platform=ios formFactor=tablet]" : {
	backgroundImage: "imgpsh_fullsize.png",
  top: "25"
}
".falabel[platform=android ]" : {
    font: {
        fontFamily: 'FontAwesome',
        height: "16",
        width: "16"
    },
    left: "10",
    color: "#FFFFFF"
}
".falabel[platform=ios formFactor=tablet]" : {
    font: {
        fontFamily: 'FontAwesome',
        height: "18",
        width: "18"
    },
    left: "10",
    color: "#FFFFFF"
}
".buttonContainer" : {

	top: "40px",

	height: "40",
	fontWeight: "35",
	bottom: "40px"
}
".button" : {
	backgroundColor: "#FFFFFF",
	width: "100%",
	height: "100%",
	textAlign: Ti.UI.TEXT_ALIGNMENT_CENTER,
	borderRadius: 7,
	font: {
		fontSize: "26"
	},
	color: "#E91C1A"
}
".textField123" : {
	color: "#FFFFFF",
	height: Ti.UI.SIZE,
	left: "30",
	width: "350",
	hintTextColor: "#FFFFFF",
	backgroundColor: "transparent",
	font: {
		fontSize: "18"
	}
},
